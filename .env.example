# Copy to .env (git-ignored) and paste your qul.tarteel.ai session cookie.
# DevTools → Application → Cookies → https://qul.tarteel.ai →
# value of `_quran_com-community_session`. Refresh it when the harvest starts
# reporting "redirect failed — logged in?".
QUL_SESSION=
